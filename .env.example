# Server-only: never use a NEXT_PUBLIC_ prefix for this key.
OPENROUTER_API_KEY=
# Only nvidia/nemotron-3.5-lightning:free or a model ending in :free is accepted.
OPENROUTER_MODEL=nvidia/nemotron-3.5-lightning:free
